{
  "name": "@authorizer/tenants",
  "version": "1.0.0",
  "description": "Lambda Authorizer Function",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --platform=node --format=esm --outfile=dist/index.mjs",
    "start:dev": "nodemon --watch src -e ts --exec \"node dist/index.mjs\"",
    "tsc": "tsc"
  },
  "dependencies": {
    "@commons/auth_manager": "workspace:*",
    "@commons/logger": "workspace:*",
    "aws-lambda": "^1.0.7",
    "jose": "^6.1.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.158"
  }
}