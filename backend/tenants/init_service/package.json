{
  "name": "@tenants/init_service",
  "version": "1.0.0",
  "description": "Lambda Function tenants_users_service",
  "type": "commonjs",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --sourcemap --platform=node --outfile=dist/index.js",
    "start:dev": "nodemon --watch src -e ts --exec \"node dist/index.js\"",
    "tsc": "tsc"
  },
  "dependencies": {
    "@codegenie/serverless-express": "4.17.1",
    "@commons/logger": "workspace:*",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.158",
    "@types/express": "^4.17.25"
  }
}